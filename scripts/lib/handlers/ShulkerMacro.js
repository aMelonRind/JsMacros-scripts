
// util.container.shulkerMacro

/** @typedef {import('../type/myTypes')} */

/** @param {Util} util */
module.exports = util => {
  if (!util?.toJava) throw new Error('util needed')

  return {
  }
}
